# Benchmarks

This subtree reserves the future layout for versioned corpora, cases, expected
semantic outcomes, minimized regressions, and machine-readable result schemas.
Phase 0 deliberately adds no fabricated benchmark, generated fixture, or
performance result.
